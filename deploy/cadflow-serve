#!/usr/bin/env bash
set -euo pipefail

APP_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
exec "$APP_DIR/.venv/bin/uvicorn" app.main:app --host "${CADFLOW_BIND_HOST:-0.0.0.0}" --port "${CADFLOW_PORT:-8080}"
